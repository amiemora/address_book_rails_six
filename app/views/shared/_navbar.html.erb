<nav class="navbar navbar-expand-lg navbar-light bg-light navb">
  <div class="container-fluid">
    <%= link_to 'Address Book', root_path, class: 'navbar-brand hoverfix'%>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      </ul>
      <form class="d-flex">
        <% if current_user %>
               <%= link_to current_user.email, user_path(current_user) %>
                <%= link_to 'Logout', logout_path, class: 'btn btn-info' %>
        <% else %>
                 <%= link_to 'Login', login_path, class: 'btn btn-info loginout'  %>
                <%= link_to 'Sign Up', signup_path, class: 'btn btn-info'  %>
        <% end %>
      </form>
    </div>
  </div>
</nav>



  

  
